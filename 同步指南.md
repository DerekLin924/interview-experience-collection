# 牛客网面经合集 · 同步指南

本仓库按「公司 / 岗位」收录牛客网专栏里的面经文章标题与原文链接。本指南说明：

1. 数据来源与抓取接口；
2. 如何用 `sync_nowcoder.py` 自动同步新文章；
3. 新增公司 / 新专栏时的处理步骤。

> 配套脚本：`sync_nowcoder.py`（与本文件同目录）。仅依赖 Python3 标准库，无需安装任何第三方包。

---

## 一、数据来源

牛客网「专栏」文章链接格式：

```
https://www.nowcoder.com/issue/tutorial?zhuanlanId={专栏ID}&uuid={文章uuid}
```

当前监控的专栏（作者：林小白zii）：

| 专栏 ID | 内容 | 文章数 |
| --- | --- | --- |
| `04ypb2` | 面经合集（字节跳动 / 阿里 / 腾讯） | 69 |

### 抓取接口（无需登录）

| 用途 | URL |
| --- | --- |
| 专栏目录（全部标题 + uuid） | `https://www.nowcoder.com/content/zhuanlan/index/catalog/{专栏ID}` |
| 单篇详情（含正文） | `https://www.nowcoder.com/content/zhuanlan/index/detail/{专栏ID}/{uuid}` |

接口返回 JSON。本脚本只用目录接口拿「标题 + uuid」，不做正文转录。

---

## 二、快速开始

```bash
cd /Users/lindiqiang/Desktop/github/interview-experience-collection   # 进入项目目录

# 1) 先检测、不修改（推荐每次第一步）
python3 sync_nowcoder.py

# 2) 确认归类无误后再重写 README.md
python3 sync_nowcoder.py --apply

# 3) 查看改动
git diff
```

脚本会打印：抓了多少篇、每篇归类到哪个「公司 / 岗位」、以及生成后的 README 预览。

---

## 三、完整操作流程

### 第 1 步：拉取最新代码

```bash
cd /Users/lindiqiang/Desktop/github/interview-experience-collection
git pull
```

### 第 2 步：检测（dry-run）

```bash
python3 sync_nowcoder.py
```

看「归类汇总」的输出，核对每篇的公司、岗位是否归类正确。

### 第 3 步：正式写入

```bash
python3 sync_nowcoder.py --apply
```

脚本会整体重写 `README.md`（按公司 → 岗位 → 编号排序）。

### 第 4 步：提交推送

```bash
git add -A
git commit -m "Add 字节跳动 面经 N 篇"
git push origin master
```

---

## 四、常见情况处理

### 4.1 新增了一个专栏（例如作者又开了「阿里面经」专栏）

1. 打开新专栏任意一篇文章，从 URL 里复制专栏 ID（形如 `0XXXXX`）。
2. 修改 `sync_nowcoder.py` 顶部的 `DEFAULT_COLUMNS`：

   ```python
   DEFAULT_COLUMNS = ["04ypb2", "新专栏ID"]
   ```

3. 或运行时临时指定，不改脚本：

   ```bash
   python3 sync_nowcoder.py --columns 04ypb2,新专栏ID
   ```

### 4.2 出现了一篇「无法归类公司」的新文章（新公司）

脚本会打印「⚠️ N 篇无法归类公司」。处理步骤：

1. 在 `sync_nowcoder.py` 的 `COMPANY_KEYWORDS` 里加一条 `("公司名", ["关键词"])`。
2. 若新公司有自己的岗位分类，在 `POSITION_KEYWORDS` 里补充岗位关键词。
3. 重新跑 `--apply`。

### 4.3 新增了一个岗位

例如字节面经出现了「前端开发岗」，在 `POSITION_KEYWORDS` 里加一条 `("前端开发岗", ["前端开发"])`。

> 注意顺序：更具体的岗位要排在更宽泛的岗位**前面**。例如「大模型算法岗」必须排在「算法岗」之前，否则「大模型算法岗」的文章会被「算法岗」先匹配掉。

---

## 五、文件结构

- `README.md`：收录全部公司 / 岗位的面经链接索引。
- `字节跳动/README.md`、`阿里/README.md`、`腾讯/README.md`：各公司子目录下的面经链接索引。
- `sync_nowcoder.py`：抓取 + 归类 + 生成 README（含各公司子目录 README）的脚本。
- `同步指南.md`：本文件。

> 即：每篇文章在 README 里一条链接，按「公司 → 岗位 → 面经编号」排序；`--apply` 会同时重写根目录 `README.md` 和各公司子目录下的 `README.md`。
